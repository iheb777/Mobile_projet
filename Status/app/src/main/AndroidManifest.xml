<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.status">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Fullrapport" />
        <activity android:name=".Adduser" />
        <activity android:name=".UpdateUser" />
        <activity android:name=".Users" />
        <activity android:name=".Info" />
        <activity android:name=".SuperAdmin" />
        <activity android:name=".SecurityManager" />
        <activity android:name=".SystemAdmin" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NetworkAdmin" />
    </application>

</manifest>